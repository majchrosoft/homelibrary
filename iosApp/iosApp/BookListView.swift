// Renamed/moved to `iosApp/iosApp/LibraryView.swift`. Empty file kept only as a
// placeholder so existing `xcodegen` project files don't error on a missing
// reference; remove after the next `xcodegen generate` regenerates the
// `iosApp.xcodeproj` from project.yml.
import Foundation
