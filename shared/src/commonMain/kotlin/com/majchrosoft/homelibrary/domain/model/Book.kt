package com.majchrosoft.homelibrary.domain.model

// The legacy `Book` data class has been replaced by `Item` to match the actual
// Firebase tree (`users/{uid}/items/{id}/{item,borrow,...}`) used by the existing
// Angular app. See [Item] for the new model. This file is kept (rather than
// deleted via the file tools) only to preserve git history; it intentionally
// declares no symbols.
