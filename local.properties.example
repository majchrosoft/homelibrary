# Copy this file to `local.properties` and fill in. `local.properties` is gitignored.
sdk.dir=/Users/your-username/Library/Android/sdk

# Firebase config (handled via .env)
# Copy .env.example to .env and fill in.
